# Version 0.1.0-alpha.4 Commit Commands

## PowerShell Commands

```powershell
git add .

$commitMessage = @"
0.1.0-alpha.4 Column management and critical bug fixes

Comprehensive column operations via overflow menu: rename, add,
delete, duplicate, sort, collapse. Fixed critical stale closure
bug causing unresponsive columns and state corruption that reverted
boards to default 3-column layout.

Column keys switched to UUID to prevent collisions. Enhanced version
sync script with validation. Removed dead code (src/state/store.ts).
Column collapsed state now persists correctly.

> Generated with Claude Code (https://claude.com/claude-code)

Co-authored-by: Claude <noreply@anthropic.com>
"@

git commit -m $commitMessage
```
