# Version 0.1.0-alpha.5 Commit Commands

## PowerShell Commands

```powershell
git add .

$commitMessage = @"
0.1.0-alpha.5 Fix ESLint pipeline error

Added scripts/** to ESLint ignore list to resolve parsing error
in CI/CD pipeline. Build scripts don't need TypeScript-aware
linting as they're Node.js tooling, not application code.

This fixes the lint stage failure where ESLint tried to parse
scripts/sync-version.js without TypeScript project configuration.

> Generated with Claude Code (https://claude.com/claude-code)

Co-authored-by: Claude <noreply@anthropic.com>
"@

git commit -m $commitMessage
```
