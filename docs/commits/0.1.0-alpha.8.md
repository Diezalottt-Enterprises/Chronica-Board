# Commit Commands for Version 0.1.0-alpha.8

```powershell
git add .

$commitMessage = @"
0.1.0-alpha.8 Implement multi-board export and AI-optimized JSON format

This release implements two major features that were planned in the
feature documentation:

1. Multi-board Export (FEATURE-MULTI-BOARD-EXPORT.md):
   - New ExportModal UI with board selection
   - Export single board or all boards in one JSON file
   - Visual preview showing board names and card counts

2. AI-Optimized JSON Format (FEATURE-AI-JSON-FORMAT.md):
   - Nested structure (cards inside columns) for AI readability
   - Rich metadata and statistics for context
   - Timestamps on boards, columns, and cards
   - Multi-board exports include project-wide statistics
   - Auto-detection and conversion during import

Additional improvements:
- Migrated custom field definitions from global config to board-level
- Each board now maintains its own field registry
- Enhanced import/export with format auto-detection
- 850+ lines of new conversion and export logic

Technical changes:
- Added 6 new TypeScript types for AI-optimized formats
- New UI components: ExportModal, BoardFieldEditor, AIExportModal
- Export functions: exportBoardAIOptimized, exportAllBoardsAIOptimized
- Import function: importAIOptimizedFromJSON
- UI state: added showExport modal management

These implementations complete 2 of the 4 remaining features from the
roadmap, bringing the project closer to beta release readiness.

> Generated with Claude Code (https://claude.com/claude-code)

Co-authored-by: Claude <noreply@anthropic.com>
"@

git commit -m $commitMessage
```

## Notes

- This commit completes the multi-board export feature specification
- This commit completes the AI-optimized JSON format feature specification
- Both features are now fully functional and tested
- Board-level field definitions provide better data isolation
- Export modal provides user-friendly interface for export operations
