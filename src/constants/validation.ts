// Validation constants for Chronica v0.1.0-alpha
// Limits for data validation and security

/**
 * Maximum file size for imports (2MB)
 */
export const MAX_FILE_SIZE = 2 * 1024 * 1024;

/**
 * Maximum number of cards per board
 */
export const MAX_CARDS = 3000;

/**
 * Maximum number of boards
 */
export const MAX_BOARDS = 20;

/**
 * Maximum tags per card
 */
export const MAX_TAGS = 50;

/**
 * Maximum title length
 */
export const MAX_TITLE_LENGTH = 200;

/**
 * Maximum description length
 */
export const MAX_DESCRIPTION_LENGTH = 2000;
