// Card rank constants for Chronica v0.1.0-alpha
// Used for ordering cards within columns

/**
 * Default rank for new cards
 */
export const DEFAULT_RANK = 1000;

/**
 * Gap between ranks when inserting at top
 */
export const RANK_GAP = 100;
